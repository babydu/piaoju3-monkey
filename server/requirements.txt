# 图像处理核心库
# 注意：在无头环境（服务器）中必须使用 opencv-python-headless
# opencv-python 需要 libGL.so.1，在无GUI的环境中不可用
opencv-python-headless>=4.9.0
numpy>=1.26.0
Pillow>=10.2.0

# OCR引擎（本地备用）
rapidocr-onnxruntime>=1.3.0

# 高级图像处理（边缘检测、图像分割等）
scikit-image>=0.22.0
scipy>=1.12.0
